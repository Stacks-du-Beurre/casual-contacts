{
    "A_Circle": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "B_Circle": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "B_Square": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "B_Polygon": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "D_Circle": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "D_Square": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "D_Polygon": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter",
    "S_Circle": "Source SVG layers paint in reverse order; flip so the blend stagger animates inward to match every other letter"
}
