# macOS
.DS_Store

# Git worktrees
.worktrees/

# Xcode
build/
DerivedData/
.derivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint

# Swift Package Manager
Packages/.build/
Packages/.swiftpm/
Packages/Package.resolved
Tools/*/.build/
Tools/*/.swiftpm/
Tools/*/Package.resolved

# Provisioning / signing
*.p8
*.p12
*.mobileprovision
CasualContacts/Config/DeveloperSettingsUpload.xcconfig

# MetricKit payload dumps pulled from device/sim — local-only, not for commit.
docs/research/metrics/

# Git worktrees (local-only isolated workspaces)
.worktrees/
Screenshots/

# Claude Code per-user state (permissions, scheduled-task locks)
.claude/

# Frame compositor venv (Pillow) and rasterized SVG cache
Tools/.venv/
Tools/frame-templates/*.png

# Designer handoff bundles — incorporated into marketing-site/ then discarded.
# Don't commit; they're scratch space. The README equivalent lives at
# docs/marketing-site-design.md.
design_handoff_marketing_site*/
.gitsecret/keys/random_seed
!*.secret
remote/developer-settings-worker/.wrangler/
